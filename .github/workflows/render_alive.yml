name: Keep Render Alive

on:
  schedule:
	- cron: '*/ * * * *'
	workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
	- name: Ping Render App
	  run: |
	    curl -s -o /dev/null -w "%{http_code}" https://convertinator.onrender.com
	    echo "Ping sent!"